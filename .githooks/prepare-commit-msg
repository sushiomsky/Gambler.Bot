#!/bin/sh
set -e

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
repo_root=$(CDPATH= cd -- "$script_dir/.." && pwd)

exec powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$repo_root/scripts/release-hooks.ps1" -HookName prepare-commit-msg "$@"
